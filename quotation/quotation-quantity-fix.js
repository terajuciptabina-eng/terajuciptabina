(()=>{'use strict';
if(!/buildplanner-v2\.html/i.test(location.pathname))return;
const RULES='calculation-rules.html',DEFAULT='../data/rates/default.json';
const N=v=>Number(v)||0,UP=v=>Math.max(0,Math.ceil(N(v))),R2=v=>Math.round(N(v)*100)/100,M2=v=>N(v)*0.09290304;
const esc=v=>String(v??'').replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;').replace(/'/g,'&#039;');
const slug=v=>String(v||'').toLowerCase().replace(/&/g,'and').replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'');
const ROOM_TYPES={living:'Living / Family',dining:'Dining',bedroom:'Bedroom',kitchen:'Kitchen',store:'Store / Utility',bathroom:'Bathroom',porch:'Car Porch / Entrance',other:'Other'};
let rules=[],rates={},busy=false,roomCounter=0;
const qtyOverride=new Map(),rateOverride=new Map(),descOverride=new Map(),excluded=new Set();
function roomType(v){v=String(v||'').toLowerCase();if(/porch|car\s*porch|entrance/.test(v))return'porch';if(/bath/.test(v))return'bathroom';if(/bed/.test(v))return'bedroom';if(/kitchen/.test(v))return'kitchen';if(/living|family/.test(v))return'living';if(/dining/.test(v))return'dining';if(/store|utility/.test(v))return'store';return'other'}
function roomOptions(){return Object.entries(ROOM_TYPES).map(([k,v])=>`<option value="${k}">${esc(v)}</option>`).join('')}
function updateRoomsEmptyState(){const c=document.getElementById('roomsContainer'),e=document.getElementById('roomsEmptyState');if(!c||!e)return;e.classList.toggle('hidden',!!c.querySelector('.room-card'))}
function addRoom(type='other',area='',forcedId='',forcedName=''){
 const c=document.getElementById('roomsContainer');if(!c)return null;roomCounter++;const id=forcedId||`room-${roomCounter}`;
 const d=document.createElement('div');d.id=id;d.className='room-card border rounded-xl p-4 bg-gray-50';
 d.innerHTML=`<div class="grid md:grid-cols-[1fr_1fr_1fr_auto] gap-4 items-end"><div><label class="block text-sm font-medium mb-2">Room Type</label><select class="room-type w-full border rounded-lg px-4 py-3 bg-white">${roomOptions()}</select></div><div><label class="block text-sm font-medium mb-2">Room / Area Name</label><input type="text" value="${esc(forcedName||ROOM_TYPES[type]||'Area')}" class="room-name w-full border rounded-lg px-4 py-3 bg-white"></div><div><label class="block text-sm font-medium mb-2">Area (sqft)</label><input type="number" min="1" step="1" value="${esc(area)}" class="room-area w-full border rounded-lg px-4 py-3 bg-white"></div><div class="no-print flex gap-2"><button type="button" class="border rounded-lg px-3 room-up">▲</button><button type="button" class="border rounded-lg px-3 room-down">▼</button><button type="button" class="border border-red-300 text-red-600 rounded-lg px-4 room-remove">Remove</button></div></div>`;
 c.appendChild(d);const s=d.querySelector('.room-type'),n=d.querySelector('.room-name'),a=d.querySelector('.room-area');s.value=ROOM_TYPES[type]?type:'other';
 s.addEventListener('change',()=>{if(!n.dataset.edited)n.value=ROOM_TYPES[s.value]||'Area';recalc()});n.addEventListener('input',()=>{n.dataset.edited='1';recalc()});a.addEventListener('input',recalc);d.querySelector('.room-remove').addEventListener('click',()=>removeRoom(id));d.querySelector('.room-up').addEventListener('click',()=>moveRoom(id,-1));d.querySelector('.room-down').addEventListener('click',()=>moveRoom(id,1));
 updateRoomsEmptyState();recalc();return id;
}
function removeRoom(id){document.getElementById(id)?.remove();updateRoomsEmptyState();recalc()}
function moveRoom(id,dir){const r=document.getElementById(id),c=document.getElementById('roomsContainer');if(!r||!c)return;if(dir<0&&r.previousElementSibling)c.insertBefore(r,r.previousElementSibling);if(dir>0&&r.nextElementSibling)c.insertBefore(r.nextElementSibling,r);recalc()}
function rooms(){const c=document.getElementById('roomsContainer');if(!c)return[];return[...c.querySelectorAll('.room-card')].map((d,i)=>{const s=d.querySelector('.room-type'),a=d.querySelector('.room-area'),n=d.querySelector('.room-name'),area=N(a?.value);return area>0?{roomId:d.id||`room-${i+1}`,label:String(n?.value||s?.selectedOptions?.[0]?.textContent||`Area ${i+1}`).trim(),area,type:roomType(s?.value||n?.value)}:null}).filter(Boolean)}
function ctx(){const rs=rooms(),porch=rs.filter(x=>x.type==='porch'),main=rs.filter(x=>x.type!=='porch'),bath=main.filter(x=>x.type==='bathroom'),bed=main.filter(x=>x.type==='bedroom'),kit=main.filter(x=>x.type==='kitchen'),A=main.reduce((s,x)=>s+x.area,0),P=porch.reduce((s,x)=>s+x.area,0),B=bath.reduce((s,x)=>s+x.area,0);return{rs,main,porch,bath,bed,kit,A,P,B,AM:M2(A),PM:M2(P),BM:M2(B)}}
function key(x,i){return`rule_${slug(x.path)}_${i}`}
function rate(x,i){return R2(rateOverride.get(key(x,i))??rates[key(x,i)]??0)}
function perimeter(area){return 4*Math.sqrt(Math.max(0,N(area)))*0.3048}
function dependency(x,c,out){const l=String(x.path||'').toLowerCase(),k=N(x.coeff)||1;if(/floor tiles.*internal|internal floor tiles/.test(l))return Math.max(0,c.AM-c.BM)*k;if(/floor tiles.*external|external floor tiles/.test(l))return c.PM*k;if(/ceiling.*internal|internal ceiling/.test(l))return Math.max(0,c.AM-c.BM)*k;if(/ceiling.*external|external ceiling/.test(l))return c.PM*k;if(/painting.*internal|internal painting/.test(l)){const q=out.find(i=>/internal wall/i.test(i.path));return q?N(q._rawQty)*k:0}if(/painting.*external|external painting/.test(l)){const q=out.find(i=>/external wall/i.test(i.path));return q?N(q._rawQty)*k:0}if(/drainage|external drain/.test(l))return perimeter(c.A)*k;return 0}
function calc(x,c,out){const m=String(x.method||'').toUpperCase().trim(),p=String(x.path||''),l=p.toLowerCase(),k=N(x.coeff),isPorch=/^porch\s*\//i.test(p),A=isPorch?c.P:c.A,AM=isPorch?c.PM:c.AM;if(m==='FIXED PER PROJECT'||m==='FIXED PROJECT')return c.rs.length?1:0;if(m==='FIXED PER BATHROOM')return c.bath.length;if(m==='FIXED PER BEDROOM')return c.bed.length;if(m==='FIXED PER KITCHEN')return c.kit.length;if(m==='FIXED PER PORCH')return c.porch.length;if(m==='FIXED PER ROOM')return c.main.length*(k||1);if(m==='FIXED PER ELIGIBLE AREA'||m==='FIXED PER DESIGNATED AREA')return c.main.filter(z=>!['bathroom','kitchen'].includes(z.type)).length*(k||1);if(m==='FIXED PER APPLICABLE LOCATION'){if(/door.*type 1|type 1.*door/.test(l))return c.main.filter(z=>['living','bedroom','kitchen','dining'].includes(z.type)).length;if(/door.*type 2|type 2.*door/.test(l))return c.main.filter(z=>['living','dining'].includes(z.type)).length;if(/door.*type 3|type 3.*door/.test(l))return c.bath.length;if(/door.*type 4|type 4.*door/.test(l))return c.main.length?1:0;if(/window.*type 1|type 1.*window/.test(l))return c.bed.length;if(/window.*type 2|type 2.*window/.test(l))return c.kit.length;if(/window.*type 3|type 3.*window/.test(l))return c.bath.length;return 0}if(m==='AREA ALLOWANCE')return(isPorch?c.porch:c.main).reduce((s,z)=>s+Math.ceil(z.area/100),0)*(k||1);if(m==='AREA / COEFFICIENT'||m==='BASELINE COEFFICIENT'||m==='COEFFICIENT'||m==='SAME AS MAIN BUILDING')return A*k;if(m==='THICKNESS'||m==='LAYER'||m==='AREA'||m==='ROOF GEOMETRY'||m==='OVERHANG AREA × THICKNESS')return AM*k;if(m==='PERIMETER × THICKNESS')return perimeter(A)*k;if(m==='PERIMETER × HEIGHT')return /bathroom/.test(l)?c.bath.reduce((s,z)=>s+perimeter(z.area)*10*0.3048,0):0;if(m==='POUNDAGE'){const parent=p.split('/').slice(0,-1).join('/').toLowerCase(),q=out.find(i=>i.path.toLowerCase().startsWith(parent)&&/concrete/.test(i.path.toLowerCase()));return q?N(q._rawQty)*k:0}if(m==='DEPENDENCY')return dependency(x,c,out);return 0}
function make(x,i,q,e={}){const id=`rule-item-${i}-${e.roomId||'project'}`,o={id,ruleIndex:i,ruleKey:key(x,i),category:x.group,description:descOverride.get(key(x,i))??x.description,unit:x.output,qty:qtyOverride.has(id)?UP(qtyOverride.get(id)):UP(q),rate:rate(x,i),amount:0,path:x.path,hierarchy:String(x.path).split('/').map(s=>s.trim()).slice(1,-1),roomId:e.roomId||'project',room:e.room||'Project',_rawQty:q};o.amount=R2(o.qty*o.rate);return o}
function build(){const c=ctx(),out=[];rules.forEach((x,i)=>{if(!x?.group||!x?.path||!x?.description||!x?.output||!x?.method)return;const m=String(x.method).toUpperCase().trim(),p=String(x.path||'').toLowerCase();if(/^FIXED PER BATHROOM$|^FIXED PER BEDROOM$|^FIXED PER KITCHEN$/.test(m)){const list=m.includes('BATHROOM')?c.bath:m.includes('BEDROOM')?c.bed:c.kit;list.forEach(z=>out.push(make(x,i,1,{roomId:z.roomId,room:z.label})));return}if(x.group==='ARCHITECTURES'&&/bathroom\s*\/\s*(wall tiles|floor tiles|ceiling)/i.test(p)){c.bath.forEach(z=>{let q=/wall tiles/i.test(p)?perimeter(z.area)*10*0.3048:M2(z.area);q*=N(x.coeff)||1;if(q>0)out.push(make(x,i,q,{roomId:z.roomId,room:z.label}))});return}const q=calc(x,c,out);if(q>0)out.push(make(x,i,q))});return out.filter(x=>!excluded.has(x.id))}
function heading(g){g=String(g||'').toUpperCase();return g==='PRELIMINARIES'?'PROJECT / PRELIMINARIES':g==='STRUCTURES'?'STRUCTURAL WORKS':g==='ARCHITECTURES'?'ARCHITECTURAL WORKS':g==='ELECTRICAL'?'ELECTRICAL WORKS':g}
function render(items){const el=document.getElementById('estimateContent');if(!el)return;const c=ctx(),total=R2(items.reduce((s,x)=>s+x.amount,0)),groups=[];items.forEach(x=>{let g=groups.find(z=>z.name===heading(x.category));if(!g){g={name:heading(x.category),nodes:[]};groups.push(g)}let node=g;x.hierarchy.forEach(h=>{let z=node.nodes.find(y=>y.name===h);if(!z){z={name:h,nodes:[],items:[]};node.nodes.push(z)}node=z});node.items.push(x)});const row=x=>`<tr class="border-b align-top"><td class="py-3 px-2">${esc(x.description)}</td><td class="py-3 px-2">${esc(x.unit)}</td><td class="py-3 px-2 text-right">${UP(x.qty)}</td><td class="py-3 px-2 text-right">${x.rate.toFixed(2)}</td><td class="py-3 px-2 text-right font-medium">${x.amount.toFixed(2)}</td></tr>`;const walk=(ns,l=0)=>ns.map(x=>`<tr class="quotation-subsection-row"><td colspan="5" class="py-2 px-2" style="padding-left:${8+l*18}px">${esc(x.name)}</td></tr>${x.items.map(row).join('')}${walk(x.nodes,l+1)}`).join('');const html=groups.length?`<div class="overflow-x-auto"><table class="w-full border-collapse text-sm detailed-quotation-table"><thead><tr class="border-b-2 text-left"><th>Description</th><th>Unit</th><th>Quantity</th><th>Rate (RM)</th><th>Amount (RM)</th></tr></thead><tbody>${groups.map(g=>`<tr class="quotation-section-row"><td colspan="5" class="py-3 px-2">${esc(g.name)}</td></tr>${walk(g.nodes)}`).join('')}</tbody><tfoot><tr class="border-t-2"><td colspan="4" class="py-4 px-2 text-right font-bold">TOTAL</td><td class="py-4 px-2 text-right font-bold text-lg">${total.toFixed(2)}</td></tr></tfoot></table></div>`:'<div class="p-4 text-sm text-gray-500 border rounded-xl bg-gray-50">No calculation items were generated from Calculation Rules.</div>';el.innerHTML=html;const tb=document.getElementById('totalBuiltArea'),rt=document.getElementById('roomsTotalArea'),gt=document.getElementById('grandTotal');if(tb)tb.textContent=`${UP(c.A)} sqft`;if(rt)rt.textContent=`${UP(c.rs.reduce((s,z)=>s+z.area,0))} sqft`;if(gt)gt.textContent=total.toFixed(2)}
function syncBuiltUp(){const t=rooms().reduce((s,r)=>s+r.area,0),e=document.getElementById('builtUpArea');if(e)e.value=String(UP(t))}
function recalc(){if(busy||!rules.length)return;busy=true;try{syncBuiltUp();render(build())}finally{busy=false}}
function parse(text){const m=/const\s+rules\s*=\s*\[/.exec(text);if(!m)throw Error('Calculation Rules data not found');const s=m.index+m[0].lastIndexOf('[');let d=0,q='',inside=false,escaped=false;for(let i=s;i<text.length;i++){const c=text[i];if(inside){if(escaped){escaped=false;continue}if(c==='\\'){escaped=true;continue}if(c===q)inside=false;continue}if(c==='"'||c==="'"||c==='`'){inside=true;q=c;continue}if(c==='[')d++;else if(c===']'&&!--d)return Function(`return ${text.slice(s,i+1)}`)()}throw Error('Calculation Rules array is incomplete')}
async function loadRules(){const a=await fetch(`${RULES}?v=${Date.now()}`,{cache:'no-store'});if(!a.ok)throw Error(`Calculation Rules unavailable (${a.status})`);const p=parse(await a.text());rules=Array.isArray(p)?p.filter(x=>x?.group&&x?.path&&x?.description&&x?.output&&x?.method):[];if(!rules.length)throw Error('Calculation Rules contains no eligible rules')}
async function loadRates(){try{const a=await fetch(`${DEFAULT}?v=${Date.now()}`,{cache:'no-store'});if(a.ok){const d=await a.json();rates=d?.rates||{}}}catch(e){console.warn('Default rates unavailable',e)}}
function ui(){window.addRoom=addRoom;window.removeRoom=removeRoom;window.moveRoom=moveRoom;window.updateRoomsEmptyState=updateRoomsEmptyState;window.updateEstimate=recalc;window.getAllItems=build;window.getCurrentQuotationData=()=>{const all=build();return{allItems:all,rooms:rooms(),total:R2(all.reduce((s,x)=>s+x.amount,0))}};window.toggleProjectInfo=()=>{const p=document.getElementById('projectInfo'),a=document.getElementById('projectArrow');p?.classList.toggle('hidden');if(a)a.textContent=p?.classList.contains('hidden')?'▼':'▲'};window.toggleRateSchedule=()=>{const p=document.getElementById('rateSchedulePanel'),a=document.getElementById('rateScheduleArrow');p?.classList.toggle('hidden');if(a)a.textContent=p?.classList.contains('hidden')?'▼':'▲'};const b=document.getElementById('addRoomAreaBtn');if(b)b.onclick=()=>addRoom();updateRoomsEmptyState()}
function bind(){document.addEventListener('input',e=>{if(e.target.closest('#roomsContainer'))recalc()},true);document.addEventListener('change',e=>{if(e.target.closest('#roomsContainer'))recalc()},true)}
async function start(){ui();bind();try{await loadRules();await loadRates();recalc();setTimeout(recalc,200);setTimeout(recalc,800)}catch(e){console.error('TERAJU Build Planner V2',e);const el=document.getElementById('estimateContent');if(el)el.innerHTML=`<div style="padding:16px;border:1px solid #fecaca;border-radius:12px;background:#fef2f2;color:#991b1b;font-weight:600">Calculation engine error: ${esc(e.message||e)}</div>`}}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',start,{once:true});else start();
})();